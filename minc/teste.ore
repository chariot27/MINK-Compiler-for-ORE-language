
impout("Media Aritmetica")
impout(" ")

func media(nota1,nota2)
    var soma_result = nota1 + nota2
    var result = soma_result / 2
    impout(result)

    if result >= 50 then impout("passou")
    if result < 50 then impout("recuperar")
    
end

media(60,60)

